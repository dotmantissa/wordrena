{
  "name": "wordrena",
  "version": "1.0.0",
  "description": "A creature battler where you write your monster's moves in plain English and GenLayer validators decide what they are worth.",
  "type": "module",
  "private": "true",
  "scripts": {
    "lint:contracts": "for f in contracts/*.py; do .venv/bin/genvm-lint check \"$f\" || exit 1; done",
    "test": ".venv/bin/pytest tests/direct -q",
    "deploy:studionet": "node deploy/deploy.mjs",
    "repair:studionet": "node deploy/repair-production.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "genlayer-js": "^1.1.8"
  }
}
